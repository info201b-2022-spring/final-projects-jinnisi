#load in data from csv file
country_data <- read.csv("/Users/alextran/Documents/info 201/Group project/final-projects-jinnisi/data/top50contry.csv")
#Add a column that extracts the state abbreviation
country_data$country <- substr(country_data$X, 1, 2)
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
#Add a column that extracts the state abbreviation
country_data$country <- substr(country_data$X, 1, 2)
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
#load in data from csv file
country_data <- read.csv("/Users/alextran/Documents/info 201/Group project/final-projects-jinnisi/data/top50contry.csv")
#Add a column that extracts the state abbreviation
country_data$country <- substr(country_data$X, 1, 2)
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
library(countrycode)
library(dplyr)
library(tidyverse)
library(ggplot2)
#load in data from csv file
country_data <- read.csv("/Users/alextran/Documents/info 201/Group project/final-projects-jinnisi/data/top50contry.csv")
#Add a column that extracts the state abbreviation
country_data$country <- substr(country_data$X, 1, 2)
#Extract crime volume by State into a new dataframe
dance_country <- country_data %>% group_by(country) %>% summarize(danceability_country = sum(dnce, na.rm = T)) %>% unique()
#Load state shapefile
world <- map_data('world')
View(country_data)
